
  # Local Sync System Dashboard

  This is a code bundle for Local Sync System Dashboard. The original project is available at https://www.figma.com/design/0QesdTMO02finF48IeYSWz/Local-Sync-System-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  